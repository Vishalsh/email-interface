import React from 'react';

import classes from './NoEmail.module.scss';

const NoEmail = (props) => (
  <p className={classes.noEmail}>There are no conversations with this label.</p>
);

export default NoEmail;
